export class Item {
    public name: string;
    public description: string;
    public imageaPath: string;

    constructor(name: string, desc: string, imagePath: string) {
        this.name = name;
        this.description = desc;
        this.imageaPath = this.imageaPath;
    }
}